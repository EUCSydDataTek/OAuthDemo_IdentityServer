<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Client.M4.MainPage"
             Title="OAuth PKCE">

    <Grid Margin="30,30,30,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" x:Name="ResultFeed" Text="Ready to start..."  Margin="0,0,10,10" />

        <StackLayout Grid.Row="1" Orientation="Horizontal">
            <Button Text="Authorize" VerticalOptions="StartAndExpand" Margin="0,0,10,0" HorizontalOptions="Start" Clicked="Authorize_Click"/>
            <Button Text="Get Token" VerticalOptions="StartAndExpand" Margin="0,0,10,0" HorizontalOptions="Start" Clicked="Token_Click"/>
            <Button Text="Call API" VerticalOptions="StartAndExpand" Margin="0,0,10,0" HorizontalOptions="Start" Clicked="Api_Click"/>
        </StackLayout>
    </Grid>

</ContentPage>
